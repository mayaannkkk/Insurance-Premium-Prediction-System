services:
  backend:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build: .
    command: streamlit run frontend.py --server.port=8501 --server.address=0.0.0.0
    ports:
      - "8501:8501"
    depends_on:
      - backend
    restart: unless-stopped